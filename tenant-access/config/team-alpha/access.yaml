# Team Alpha access configuration
# This file defines AI model access for Team Alpha

team:
  name: alpha
  displayName: "AI Access - Team Alpha"
  owner: alpha-lead@contoso.com
  costCenter: CC-12345

models:
  # Foundry models - list of models with quotas
  foundry:
    - name: gpt-4.1
      tokensPerMinute: 10000
      dailyTokenQuota: 1000000

  # KAITO models - references to platform-managed shared models
  # Empty list means no KAITO model access
  kaito: []
