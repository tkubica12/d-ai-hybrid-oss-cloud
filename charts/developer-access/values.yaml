# Team configuration
team:
  name: ""           # Required: team name (e.g., "alpha")

# KAITO workspace configuration for dedicated GPU compute with open-source models
kaito:
  enabled: false
  # KAITO workspace definitions (creates dedicated GPU compute)
  workspaces: []
  # Example:
  # - name: phi-4
  #   instanceType: Standard_NC24ads_A100_v4
  #   labelSelector:
  #     matchLabels:
  #       apps: phi-4

